export enum TradingCountry {
  US = 'US',
  BR = 'BR',
}

export enum TransactionType {
  Buy = 'Buy',
  Sell = 'Sell',
}

export enum PositionType {
  Stocks = 'Stocks',
  REITs = 'REITs',
  Bonds = 'Bonds',
}
